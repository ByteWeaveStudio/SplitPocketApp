# macOS
.DS_Store

# Claude / local tooling
docs/
*.md
!README.md
.claude/
.impeccable/

# Environment files. Nothing in the app reads one — the Firebase web config
# lives in src/services/firebase.ts — but keep these ignored so a stray local
# file never lands in a commit.
.env
.env.*

# Firebase CLI
.firebase/
firebase-debug.log
